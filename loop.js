/*
for (let i = 0; i <= 10; i += 2) {
  console.log(i);// include your code here
}

for (let i = 10; i >= 1; i -= 1) {
  console.log(i);
}
console.log('Launch!');


let greeting = 'Aloha!';

for (let i = 0; i < 3; i += 1) {
  console.log(greeting);
} 

//Write a for loop that iterates over all numbers from 1 to 100, and outputs 
//the result of multiplying each element by 2.

for (let count = 1; count < 101; count +=1) {
  console.log(count * 2);
}

//write a while loop that logs the elements of array at each index, and 
//terminates after logging the last element of the array.

let array = [1, 2, 3, 4];
let index = 0;

while (index < array.length) {
  console.log(array[index]);
  index += 1;
}

//write a for loop that loops over the elements of the array cities and logs 
//the length of each string to the console. If the element is null, skip 
//forward to the next iteration without logging anything to the console.

let cities = ['Istanbul', 'Los Angeles', 'Tokyo', null, 'Vienna', null, 'London', 'Beijing', null];

for (let i = 0; i < cities.length; i += 1) {
  if (cities[i] === null) {
    continue;
  }
   console.log(cities[i].length);
}

//modify it so that it stops after the first iteration.

for (let i = 0; ; i += 1) {
  console.log("and on");
  break;
}

//Write a while loop that logs all odd natural numbers between 1 and 40.

let count = 1;

while (count < 41) {
  if (count % 2 !==0) {
    console.log(count);
  }
  count +=1;
}

//Loop over the elements of the array fish, logging each one. 
//Terminate the loop immediately after logging the string 'Nemo'.

let fish = ['Dory', 'Marlin', 'Gill', 'Nemo', 'Bruce'];
let count = 0;

while (count < fish.length) {
  console.log(fish[count]);
  if (fish[count] === 'Nemo') {
    break;
  }
  count += 1;
}

*/




















































